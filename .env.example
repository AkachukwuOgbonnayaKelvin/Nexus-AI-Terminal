# Database connection string (replace with your actual credentials)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/nexus_ai_terminal

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
