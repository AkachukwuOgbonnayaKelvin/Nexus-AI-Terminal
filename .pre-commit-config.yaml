# Pre-commit hooks with ARC-001 integration
repos:
  - repo: local
    hooks:
      - id: arc-001
        name: ARC-001 Architecture Compliance
        entry: python tools/architecture/engine.py
        language: python
        pass_filenames: false
        stages: [commit]
        always_run: true
        verbose: true
